Finance Tracker — living handoff (read/save to resume work)
===========================================================
Last updated: 2026-05-16 (v79: repeat income/petrol, month summary, import review)

<<<MEMO_AUTO_SUMMARY>>>
Generated: 2026-05-14T16:32:54.765Z

Recent commits (newest first):
  - 912bdc3 (2026-05-15) feat(charts): daily NAV carry-forward + portfolio line by day; expense daily spend line
  - 087aa9e (2026-05-15) feat(sync): hard-coded Apps Script URL; read-only display on Settings; drop stored URL override
  - 377b02b (2026-05-14) chore(sync): new default Apps Script web app URL; bump v62 / sync v61 / ft-v62
  - d7ea386 (2026-05-14) chore(ui): BUILD_VERSION shows date + 24h time; bump v61 / ft-v61
  - a20a563 (2026-05-14) docs(memo): refresh auto git summary
  - c37ffa2 (2026-05-14) chore(memo): auto git log block + update script and optional post-commit hook
  - e6c677c (2026-05-14) Sync: auto-pull on focus, Sheet-overwrite UT, in-flight guard; shorter save error copy; v60 cache bump
  - 2879723 (2026-05-14) Speed up Sheet sync: parallel save_chunk batches, larger chunks, faster auto-load/debounce; extension POST for large saves
  - 2e78628 (2026-05-14) Auto-load Sheet on open (autoStart bypasses empty-cloud skips); silent pull; pull after URL change; bump v56
  - 30ed7ef (2026-05-14) Stop SW controllerchange reload loop after nosw / cache clear (sw_reload=0 + debounce)
  - 5ec5189 (2026-05-14) Fix Apps Script normalizeAction when e.parameters.action is a string (save_chunk); apiVersion 5
  - e453644 (2026-05-14) Fix auto-load race so Sheet data and UT holdings sync on PWA; clarify save_chunk errors; strip /exec URL on save/load
  - 51008ad (2026-05-14) Merge post-pull stash: sync diagnostics, flush bottom nav, memo notes
  - 9918c18 (2026-05-14) docs(memo): document UT charts, daily line charts, search focus
  - 3e353c8 (2026-05-14) fix(a11y): move focus before aria-hidden on expense search popover
  - 01c5b29 (2026-05-14) feat(unit-trust): total paid RM (totalCost) instead of avg per unit
  - 3d692a0 (2026-05-14) fix(sync): surface api v3 and UT sheet tab guidance
  - 2663590 (2026-05-14) fix(settings): full-width backup/sync rows and stacked actions
<<<END_MEMO_AUTO_SUMMARY>>>

PURPOSE
-------
- Offline-first PWA: static HTML/CSS/JS, installable; PWA persists via `localStorage` (see `chromeStorage` adapter at top of `app.js`). Extension uses `chrome.storage.local`.
- Optional cloud: Google Apps Script backend; user deploys `google-apps-script.js` from repo root (keep in sync with `sync.js`).
- Mirror: `finance-tracker-chrome/` — same UI logic; `chrome.storage` + extension background for Sheets fetch (host_permissions avoid PWA POST/redirect body loss).

STACK
-----
- `index.html` — structure, most CSS, inline boot/lock handling, `BUILD_VERSION`, script tags with `?v=`.
- `app.js` — data model, persistence hooks, `fmt`, bar charts, `renderTxList`, `activateNavTab`, `EXP_CAT_GROUPS`; **unit trust** (`utSanitizeHoldings`, `utCostBasis`, `utBuildPortfolioSeries`, `utCarryForwardNavSnapshotForToday`, `renderUtChart`); **month daily line charts** (`buildMonthDateKeys`, `buildMonthDailyTotals`, `renderMonthDailyLineChart`).
- `features.js` — global search (`#search-input`, top sheet from 🔍 on Expenses, `renderSearch`, `setExpSearchOpen` / `openExpSearch` / `closeExpSearch`), optional auto-backup UI + daily trigger, dedupe for import, Ctrl+F → open search on Expenses.
- `extras.js` — supplementary behaviours (keep in parity when touching related flows).
- `sync.js` / `finance-tracker-chrome/sync.js` — Sheet pull/push (`buildPayload`), chunked save; **Apps Script web app URL is hard-coded** (`APPS_SCRIPT_WEB_APP_URL`); Settings shows read-only `#sync-url-display` (no user URL field). Clears legacy `sync_url_v1` on load. **Ping** returns `apiVersion`: hints if <3 (UT tabs) or <4 (**UTHoldings.totalCost**). Auto-load on `ft-app-ready`, **visibilitychange** / **pageshow** (bfcache) debounced pull; **`syncLoadInFlight`** avoids overlapping loads. **Unit trust** in payload: `UTHoldings` / `UTNav`.
- `import-inline.js` — TNG/UOB-style wizard; precached by SW.
- `service-worker.js` — `CACHE` name + `SHELL` list (must match files you ship offline).
- Recovery: `nosw.html` (clear SW/cache), `lock.html` (legacy redirect recovery; primary UX uses `#ft-boot` overlay in index).

NAVIGATION
----------
- SPA: one `index.html`; sections are `.page` blocks with ids `page-*` (only one has `.active`).
- Desktop: left sidebar `.sb` / `.sb-nav` shows all primary nav buttons.
- Mobile: `.sb` is a fixed bottom bar. Items with `data-mobile-overflow="1"` are hidden on small viewports; **More** (`#nav-more-open`) opens `#mobile-nav-more-sheet` for Recurring, Trends, Petrol, Report.
- **Import** (statement wizard + JSON backup) lives under **Settings**, not a top-level nav page.
- **Search**: 🔍 on **Expenses** opens a top sheet (`#exp-search-pop`) with `#search-input` / `#search-results`; Ctrl+F same. No separate Search page. On close, focus leaves the sheet **before** `aria-hidden` is set (`setExpSearchOpen`); on open, `#search-input` is focused inside a short `setTimeout` and only if the sheet stayed open (`openExpSearch` guard; Ctrl+F uses a slightly longer delay).
- Bottom nav uses `flex` / `space-between` for visible primary tabs (not scroll-snap in current CSS).

RECENT PRODUCT (verified in repo; omit rumours)
-----------------------------------------------
- Mobile-only “app shell” styling: elevated cards, pill controls, floating bottom nav capsule, dark “spent this month” hero — see `@media` blocks and comments around v16/v22 in `index.html`.
- Expenses (mobile): card order via flex `order` (add form first, stats, list, budget, chart); FAB scrolls to amount field; compact stat row.
- Expense category grid density adjusted (mobile CSS; comments reference column counts).
- Income list prefixes amounts with `+ `; **expense** list uses `fmt(amount)` only — **no leading minus** on list rows (pending if product wants “−RM …”).
- **Auto-backup** toggle + daily-on-open logic still present in `features.js` / Settings UI — **not removed**.
- PWA Sheets: large saves use chunked GET reassembly (see `sync.js` + deployed script); small payloads may use single GET save; redeploy Apps Script after backend changes.
- **Unit trust (Assets):** Holdings **`totalCost`** = lump sum paid **incl. fees**. **`utCarryForwardNavSnapshotForToday`** (after load + each `render`): once per calendar day (`localStorage` **`ft_ut_nav_snap_ymd`**), adds **today** NAV rows per fund from latest NAV so history grows daily; **`saveUtNav`** triggers debounced Sheet save. **`utBuildPortfolioSeries`**: calendar walk from earliest NAV/purchase date through **today**, sum **units × `utNavAsOf`** per day (skips funds with no NAV that day). **`renderUtChart`** on `#ut-chart` (built in `renderUnitTrustPanel`). Heading explains carry-forward.
- **Expenses:** **“Spending by day this month”** card with **`#exp-daily-chart`** — SVG line via **`buildMonthDateKeys`**, **`buildMonthDailyTotals`**, **`renderMonthDailyLineChart`** (current month only). Income page has **no** daily line chart yet (only bar chart).

IMPORTANT IDS / HOOKS
---------------------
- Pages: `page-expenses`, `page-income`, `page-assets`, `page-recurring`, `page-trends`, `page-settings`, `page-petrol`, `page-report`. **Expenses** includes **`#exp-daily-chart`** (daily spend line for visible month). **Assets** unit trusts: `#ut-root`, `#ut-summary-mv`, `#ut-chart`; storage `unit_trust_holdings_v1`, `unit_trust_nav_v1`. **Settings** Google sync: read-only **`#sync-url-display`**, `#sync-ping-btn`, `#sync-save-btn`, `#sync-load-btn`.
- Boot / readiness: `#ft-boot`, `#ft-boot-msg`, `ft-app-ready` (dispatched when app finished init).
- Tab switch: `activateNavTab('expenses'|...)` in `app.js` (toggles `.active` on `#page-<tab>` and nav buttons).
- Categories: `EXP_CAT_GROUPS` + `renderExpCatGrid` (and related) in `app.js`.
- Cache bust display: `BUILD_VERSION` in `index.html`; footer `#lk-ver` text set from it.

HOW TO CONTINUE
---------------
0. **Auto git summary in this file** — Between `<<<MEMO_AUTO_SUMMARY>>>` and `<<<END_MEMO_AUTO_SUMMARY>>>` is regenerated from `git log` by `node scripts/update-memo-summary.mjs`. To refresh after every local commit, run once: `git config core.hooksPath hooks` (repo `hooks/` folder). The `post-commit` hook runs that script and, if `memo.txt` changed, creates a follow-up commit `docs(memo): refresh auto git summary`. To skip hook: `SKIP_MEMO_HOOK=1 git commit …`. To refresh memo only without the extra commit: `MEMO_HOOK_NO_AUTOCOMMIT=1 git commit …` or run the node script manually.
1. After changing precached assets: run **`node scripts/bump-build.mjs <version>`** (updates `build-id.js`, `service-worker.js` CACHE, `BUILD_VERSION`, all `?v=` in `index.html` + `finance-tracker-chrome/app.html`). Add `build-id.js` to extension packaging if used there.
2. If `google-apps-script.js` changes: user must create a new Apps Script deployment version.
3. Repo rule (`.cursor/rules`): when finishing a fix, `git add` relevant files, **commit**, **push** `origin/main` unless user says stop or push fails.

HISTORY / GOTCHAS (short)
-------------------------
- Past white-screen: unclosed `<style>` in head — entire body treated as CSS; always validate HTML structure after edits.
- iOS: avoid `body { position:fixed }` + nested scroll traps; prefer normal document scroll + bottom padding for nav.
- PWA POST to `script.google.com` can lose body on redirect; extension avoids this; PWA uses GET/chunk strategy.

ROADMAP (ideas backlog)
-----------------------
Done (2026-05-16 batch):
  [x] Tab render registry (`TAB_OPEN_HOOKS`, `runTabOpenHooks`, `refreshActiveTabPanels`)
  [x] Sync detail line in Settings (`#sync-detail`, counts + relative times)
  [x] Mobile FAB quick-add menu + repeat last expense
  [x] Budget alert chips on Expenses (>=75% of limit)
  [x] Auto net worth snapshot on bank balance change (`settings.nwAutoSnapshot`)
  [x] More menu sheet fix (category modal HTML + body portal)
  [x] Single build id (`build-id.js`, `node scripts/bump-build.mjs`)
  [x] GitHub Action `node --check` on core JS
  [x] Category rules (learn on add, suggest on type/blur) — `cat_rules_v1`
  [x] Duplicate expense confirm on add
  [x] Budget chip tap filters transaction list
  [x] Repeat last income + petrol (FAB quick-add)
  [x] Month summary chips on Expenses (budget headroom + net)
  [x] Import review: row checkboxes, select all/none, stable row ids
  [x] Swipe edit/delete on mobile transaction rows
  [x] Trends tap month → income breakdown by category
  [x] Savings goal card on Expenses (target RM by date)
  [x] Net worth stale hint at 14+ days (Assets + Trends)
  [x] Maybank / CIMB CSV import source
  [x] Swipe actions on search, recurring, and petrol lists

Stability / tooling (todo):
  [ ] UTF-8 safe edits only (no PowerShell bulk replace on HTML)
  [ ] Pre-release smoke: More sheet, Trends, add expense, sync line
  [ ] Scan/fix remaining corrupted UI strings (em dash / closing tags)

Daily logging (todo):
  [x] Repeat last for income + petrol (FAB + storage templates)
  [x] Month summary chip on Expenses hero (“left in budgets” / net)
  [x] Swipe edit/delete on transaction rows (mobile)

Budgets & trends (todo):
  [x] Trends month column → income breakdown
  [x] Gentle net worth reminder if stale (14+ days)
  [x] Simple savings goal (target RM by date)

Imports & Sheets (todo):
  [x] One more bank CSV (Maybank / CIMB)
  [x] Import review screen (checkboxes, select all/none, category fix)
  [ ] Clearer sync conflict copy (device vs cloud counts)
  [ ] Monthly backup export reminder

Polish (todo):
  [ ] Dark mode schedule
  [ ] Share/print monthly report improvements
  [ ] Petrol L/100 + RM/km on Expenses hero when month has fills

Open
----
- After backend deploy, confirm Pages shows new `BUILD_VERSION` and SW picked up new `CACHE` (hard refresh / `nosw.html` if stuck). Bump: `node scripts/bump-build.mjs <n>`.
- If save still fails on the website: check `localStorage` key `ft.lastSyncError`; redeploy `google-apps-script.js` so `save_chunk` is live. **Sync URL** is no longer user-editable — change **`APPS_SCRIPT_WEB_APP_URL`** in both `sync.js` files and ship a new build.
